<a href="/cad" type="submit" class="btn btn-outline-primary float-right">Inserir tarefa</a>
<h1 class="text-center">Lista de tarefas: </h1>
{{#each posts}}
	<a href="/deletar/{{dataValues.id}}"><button class="btn btn-outline-danger float-right"> Deletar </button></a>
    <a href="/editar/{{dataValues.id}}"><button class="btn btn-outline-primary float-right"> Editar </button></a>

   {{!--  <small>{{dataValues.createdAt}}</small> --}}
    <h1>{{dataValues.titulo}}</h1>
    <p>{{dataValues.conteudo}}</p>
    
    <hr>
{{/each}}